// A simple global holder for the background audio instance
// This is a singleton — the same instance is shared across all files that import it

const bgAudio = {
    instance: null,
};

export default bgAudio;